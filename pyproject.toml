[project]
name = "maintenance-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=3.13.0",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
maintenance-ai = "maintenance_ai:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
